#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

node --version >/dev/null
pnpm --version >/dev/null
printf '[orb resume] Toolchain ready\n'
