export const metadata = { alternates: { canonical: 'https://cuvii.dev/flapkit/experiments/' } }

import { ExperimentsPage } from '../../modules/experiments'

export default function ExperimentsRoute() {
  return <ExperimentsPage />
}
