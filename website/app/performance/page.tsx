export const metadata = { alternates: { canonical: 'https://cuvii.dev/flapkit/performance/' } }

import { PerformancePage } from '../../modules/performance'

export default function PerformanceRoute() {
  return <PerformancePage />
}
